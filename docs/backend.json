
{
  "entities": {
    "Score": {
      "title": "Score",
      "type": "object",
      "description": "Represents a player's score in the Emoji Match Challenge game.",
      "properties": {
        "playerName": {
          "type": "string",
          "description": "The name of the player."
        },
        "totalTime": {
          "type": "number",
          "description": "The total time taken by the player to complete the game, in seconds."
        },
        "moves": {
          "type": "number",
          "description": "The number of moves taken by the player to complete the game."
        },
        "submissionDate": {
          "type": "string",
          "description": "The date and time when the score was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "playerName",
        "totalTime",
        "moves",
        "submissionDate"
      ]
    },
    "Admin": {
      "title": "Admin",
      "type": "object",
      "description": "Represents an admin user.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The unique identifier of the admin user."
        }
      },
      "required": ["uid"]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
      "structure": [
        {
          "path": "/scores/{scoreId}",
          "schema": {
            "$ref": "#/entities/Score"
          },
          "description": "Stores individual player scores. Allow public create and read access for all users."
        },
        {
          "path": "/admins/{adminId}",
          "schema": {
            "$ref": "#/entities/Admin"
          },
          "description": "Stores admin user IDs. Used for access control in security rules."
        }
    ]
  }
}
